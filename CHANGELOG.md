# Changelog

## Unreleased

### Changed

- Replace the monolithic installer with a validated platform manifest, native format adapters, staged ownership-safe writes, generated support documentation, and a cross-platform contract test suite. ([#21](https://github.com/ciampo/ai-instructions/pull/21))
- Convert workflows to standard Agent Skills, add native custom-agent support for every configured platform, and separate release preparation from publication. ([#20](https://github.com/ciampo/ai-instructions/pull/20))
- Add the modernization roadmap, run installer tests in CI, make `check` fail for stale or conflicting managed files, correct the WCAG target-size guidance, and update the Markdown tooling dependency. ([#19](https://github.com/ciampo/ai-instructions/pull/19))
