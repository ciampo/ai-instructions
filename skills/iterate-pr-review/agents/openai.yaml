interface:
  display_name: "Iterate PR Review"
  short_description: "Iterate PR feedback until reviews converge"
  default_prompt: "Iterate on this pull request through up to three change rounds: request Copilot and independent reviews, apply accepted fixes, commit, push, and refresh the PR; then report feedback remaining after the final review-only pass."
