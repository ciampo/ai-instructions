interface:
  display_name: "Iterate PR Review"
  short_description: "Iterate PR feedback until reviews converge"
  default_prompt: "Use $iterate-pr-review to request exact-head Copilot reviews, fix findings, verify, commit, push, and repeat until clean."
