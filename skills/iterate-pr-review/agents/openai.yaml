interface:
  display_name: "Iterate PR Review"
  short_description: "Iterate PR feedback until reviews converge"
  default_prompt: "Use $iterate-pr-review to review this pull request with Copilot and an independent pass. Make changes or update the remote only when those actions are explicitly authorized, and report any feedback that remains."
