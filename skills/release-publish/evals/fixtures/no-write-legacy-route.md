# Legacy release-route context

The current package version is `4.1.2`. The unreleased changelog contains a compatible bug fix, so the repository's semantic-versioning policy would recommend `4.1.3` when release preparation is authorized.

The requester deliberately used the deprecated `release-publish` name, but requested planning only. They explicitly prohibit file changes, temporary or plan files, commits, tags, pushes, publication, and remote-release creation.
