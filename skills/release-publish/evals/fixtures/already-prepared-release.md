# Already prepared release context

Release `4.1.3` has already completed its approved local preparation: version metadata and the changelog are final, and all required verification passed. No local files may be modified further.

The requester invokes the legacy `release-publish` name and explicitly authorizes publication of this already prepared release. The request may create the authorized remote release artifacts, but it must not restart local preparation or create a plan file.
