# Setup guide context

Review this version-controlled documentation and source as a read-only writing exercise.

The command accepts `--copy` to install copies and `--agent <name>` to select a target. It does not accept `--all`.

```md
## Install

Run `tool setup --all` to install everywhere. This command is very important because it performs an important setup operation that sets up the setup for you.
```

```js
// Increment index by one so that index becomes the next index after the current index.
index += 1;
```
