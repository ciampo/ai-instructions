# Authored pull-request title workflow context

The session's primary task is authoring pull request 81234. Its canonical title is `Button: Preserve shortcut descriptions for icon-only controls`. The current task title is `🛠️ [A#81234] Button: Preserve shortcut descriptions for icon-only controls`.

The user later asks the agent to address review feedback, rebase the pull request, and open a supporting follow-up issue for a separate migration. These are later workflow steps and supporting work for the same authored pull request. The user does not replace the primary task.

Choose the task title. Return only the title.
