# GitHub issue title context

The user asks the agent to investigate GitHub issue 654. Its canonical title is `Login redirects loop after expired sessions`. Its description explains that affected users are redirected between the login and account pages after their session expires.

The runtime supports task titles. No title has been set yet.

Choose the initial task title. Return only the title.
