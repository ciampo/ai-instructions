# Co-equal GitHub task-title context

The session's primary task begins as a review of pull request 80600, whose canonical title is `Theme: Omit color properties when neither provided nor inherited`. The current task title is `👀 [R#80600] Theme: Omit color properties when neither provided nor inherited`.

The user then adds issue 700, whose canonical title is `Theme: Remove obsolete contrast fallback`, and explicitly says the issue and pull-request review are now co-equal primary tasks. Neither task dominates. This is not a supporting follow-up.

Choose the task title. Return only the title.
