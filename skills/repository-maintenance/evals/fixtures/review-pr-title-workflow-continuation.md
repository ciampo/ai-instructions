# Review pull-request title workflow context

The session's primary task is reviewing Gutenberg pull request 80600. Its canonical title is `Theme: Omit color properties when neither provided nor inherited`. Its description explains that the change prevents `ThemeProvider` from emitting color properties when no color value is provided by props or an inherited theme.

The current task title is `👀 [R#80600] Theme: Omit color properties when neither provided nor inherited`.

The user asks the agent to iterate the review and verify the updated pull-request head after findings are addressed. These are later workflow steps for the same primary review. The user does not replace the primary task.

Choose the task title. Return only the title.
