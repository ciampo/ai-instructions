# GitHub issue-to-pull-request title context

The session begins with an investigation of GitHub issue 654. Its canonical title is `Login redirects loop after expired sessions`. The current task title is `🔍 [I#654] Login redirects loop after expired sessions`.

The user asks the agent to implement the fix and open authored pull request 655. Its canonical title is `Login: Prevent redirect loops after expired sessions`. The authored pull request is now the session's primary delivery for the issue. The user does not retain the issue investigation as a separate co-equal objective.

Choose the task title. Return only the title.
