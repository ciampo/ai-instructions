# GitHub task-title fallback context

The user asks to investigate GitHub issue 321. They provide the title "Login redirects loop after expired sessions" and explain that affected users are redirected between the login and account pages after their session expires.

A read-only attempt to fetch the canonical issue metadata is unavailable. No other issue details have been verified.

Choose the task title. Return only the title.
