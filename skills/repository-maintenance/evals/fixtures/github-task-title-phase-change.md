# GitHub task-title phase-change context

The primary task is reviewing Gutenberg pull request 80600. Its canonical title is "Theme: Omit color properties when neither provided nor inherited." Its description explains that the change prevents `ThemeProvider` from emitting color properties when no color value is provided by props or an inherited theme.

The current task title is `👀 [R#80600] Omit unused theme color properties`.

The review findings have been addressed. The user asks the agent to verify the updated pull-request head. This is a materially different phase of the same primary task, not a new objective.

Choose the task title for the verification phase. Return only the title.
